services:
  postgres:
    image: postgres:17
    network_mode: "host"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    volumes:
      - pg_data:/var/lib/postgresql/data

  redis:
    image: redis:7
    network_mode: "host"

volumes:
  pg_data:
    
