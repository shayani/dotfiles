# Personal development settings override
# Add your own custom volume mounts and environment variables here
# DO NOT commit this file with your personal settings to the repository.
# If necessary, add this file to your .git/info/exclude file.

services:
  web-app:
    # Add personal overrides here
    # Example volume mounts (uncomment and modify as needed):
    volumes:
      - ~/.ssh/id_ed25519:/home/vscode/.ssh/id_ed25519:ro
      - ~/.config:/home/vscode/.config:ro
    #   - ~/.gitconfig:/home/vscode/.gitconfig:ro
    #   - ~/.aws:/home/vscode/.aws:ro

    # Example environment variables:
    # environment:
    #   - CUSTOM_VAR=value
